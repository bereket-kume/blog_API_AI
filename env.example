# MongoDB Configuration
# For local MongoDB (default) - Docker setup
MONGODB_URI=mongodb://admin:password123@mongodb:27017/blog_db?authSource=admin

# For MongoDB Atlas (uncomment and replace with your connection string)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/blog_db?retryWrites=true&w=majority

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Application Configuration
PORT=8080
ENV=production

# Local MongoDB Configuration (for Docker setup)
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=password123
MONGO_DATABASE=blog_db
